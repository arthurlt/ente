---
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
  # assumes there will be a ConfigMap named ente-museum-config
  - target:
      kind: Deployment
      name: ente-museum
    path: add-volume.patch.yaml