apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: ente-database-backup
  labels:
    app.kubernetes.io/component: database
spec:
  # midnight daily
  schedule: "0 0 0 * * *"
  backupOwnerReference: self
  cluster:
    name: ente-database
  method: barmanObjectStore
